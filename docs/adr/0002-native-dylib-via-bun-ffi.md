# Native ObjC++ dylib via bun:ffi for image conversion and Photos edits

The original implementation shelled out to `osxphotos`, `sips`, and `qlmanage` for HEIC→JPEG conversion, thumbnailing, video frame extraction, and writing edits back to Photos.app — a Python toolchain on `pipx` plus subprocess overhead per image. We replaced this with `resources/native/karttapallo-bridge.mm` (ObjC++ over ImageIO / AVFoundation / NSAppleScript) loaded via `bun:ffi`. Image operations are now in-process and roughly an order of magnitude faster; edit writes use `NSAppleScript` directly with a main-thread guard. The dylib is built by `bun run build:native` and bundled into the app at `Resources/app/`. Trade-off: macOS-only and a small C++ surface to maintain — but the project was always Apple-Photos-only, so portability isn't a goal.
